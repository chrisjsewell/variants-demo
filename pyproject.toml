[project]
name = "variants-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sphinx-needs",
]

[tool.uv.sources]
sphinx-needs = { git = "https://github.com/useblocks/sphinx-needs", branch = "master" }
